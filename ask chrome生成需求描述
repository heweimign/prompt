请根据我当前打开的原型页面，直接整理该页面对应后端接口所需的入参和出参字段信息。我的完整需求是“工具申请”、“工具申请”下面的“新增申请”和“申请详情”，共3个菜单。

要求：
- 只基于当前页面可见内容输出；
- 用中文，简洁清晰；
- 只写页面对应接口需要的入参、出参；
- 字段只写中文名称，并说明字段用途、是否必填（如可判断）、返回或使用场景；
- 不要写页面概述、主要功能、用户流程等无关内容；
- 不要输出英文参数名、URL、请求方式、表结构、代码等技术设计；
- 可以合理推断；
- 当前页面无法确认但影响接口开发的内容，放到“待确认项”里；
- 如果后续我打开新页面，请在已有结果上继续补充，不要推翻重写。
- 不要用表格，用文本描述即可


输出格式：
1. 页面用途
2. 接口入参信息
3. 接口出参信息
4. 待确认项
