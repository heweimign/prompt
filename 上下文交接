当前 Codex 会话的上下文快满了。请你停止继续实现代码，先基于本会话到目前为止的全部上下文，生成一份“新会话接手提示词”。

这份提示词的目标是：我可以把它完整复制到一个新的 Codex 会话中，让新的 Codex 会话不依赖旧会话上下文，也能继续完成剩余开发任务。

请注意：你不是简单总结，而是要生成一段可以直接给新 Codex 使用的完整提示词。

请严格按照下面要求生成。

---

# 你需要输出的内容

请输出一段完整的 Markdown 提示词，标题为：

# 新 Codex 会话接手提示词

这段提示词需要使用“对新 Codex 说话”的口吻来写，比如：

“你现在要接手一个进行到一半的开发任务……”

而不是用“上一个会话做了什么”的旁观者口吻。

---

# 新提示词必须包含以下信息

## 1. 角色与任务定位

说明新 Codex 现在要做什么，例如：

- 你正在接手一个已经开发到一半的项目
- 旧会话上下文即将满，所以需要你根据这份交接信息继续开发
- 你的目标是继续完成剩余任务，而不是重新开始
- 你需要先理解现有代码，再继续修改

## 2. 项目基本信息

请根据当前会话上下文自动填写：

- 项目名称
- 项目用途
- 项目路径
- 技术栈
- 运行环境
- 包管理工具
- 启动命令
- 构建命令
- 测试命令
- lint / typecheck 命令
- 其他重要命令

如果某项信息当前上下文没有，请写：

“当前上下文未明确，请新会话先检查项目文件确认。”

不要编造。

## 3. 原始任务目标

请完整还原我最初让你做的任务，包括：

- 最终要实现什么
- MVP 范围是什么
- 明确不做什么
- 用户/产品目标是什么
- 验收标准是什么

如果本会话中我曾经修改过需求，请以最新需求为准。

## 4. 已经完成的工作

请具体列出：

- 已完成的功能
- 已创建的文件
- 已修改的文件
- 已安装的依赖
- 已调整的配置
- 已修复的问题
- 已经验证通过的内容

要求尽量具体到文件路径和功能点，不要只写泛泛总结。

## 5. 当前代码状态

请说明：

- 当前项目是否能启动
- 当前是否能 build
- 当前是否有报错
- 当前有哪些功能可用
- 当前哪些功能只是部分完成
- 当前哪些地方需要特别小心

如果你无法确定，请明确写“不确定，需要新会话检查”。

## 6. 关键文件说明

请列出关键文件，并说明每个文件的作用。

格式示例：

- `src/App.tsx`：主页面入口，负责 xxx
- `src/components/FoodForm.tsx`：负责 xxx
- `src/lib/storage.ts`：负责 xxx

只列真正重要的文件，不要把无关文件全部列出来。

## 7. 剩余未完成任务

请按优先级列出接下来要做的事情。

要求：

- 必须具体
- 必须可执行
- 不要写模糊任务
- 不要加入我没有要求的新功能
- 如果某任务依赖前一个任务，请说明顺序

格式示例：

1. 修复 xxx 报错
2. 完成 xxx 页面
3. 补充 xxx 状态处理
4. 运行 xxx 命令验证

## 8. 当前问题、报错与注意事项

如果当前会话中出现过报错，请完整保留：

- 报错命令
- 报错信息
- 已尝试的解决方案
- 还没解决的原因
- 新会话应该如何继续排查

如果没有报错，也请写明：

“当前没有已知未解决报错。”

## 9. 已执行过的命令

请列出本会话中已经执行过的重要命令，以及结果。

格式示例：

```bash
npm install
# 结果：成功 / 失败，说明原因
```

```bash
npm run build
# 结果：失败，错误是 xxx
```

如果没有执行过命令，请写明。

## 10. 开发约束

请从当前会话中总结所有必须遵守的约束，例如：

- 不要重做已完成工作
- 不要重新初始化项目
- 不要引入无关依赖
- 优先完成 MVP
- 不要做登录注册
- 不要接数据库
- 不要重构无关代码
- 不要删除用户已有文件
- 不要执行高危命令
- 修改前先查看文件
- 修改后要运行验证命令

请结合当前项目实际情况生成，不要机械套模板。

## 11. 新会话开始后的执行步骤

请明确告诉新 Codex 开始后应该怎么做。

要求必须包含：

1. 先查看项目结构
2. 再阅读关键文件
3. 对照交接信息判断当前状态
4. 不要重复已完成内容
5. 继续完成剩余任务
6. 每次修改后说明改了什么
7. 最后运行验证命令并汇报结果

## 12. 最终交付格式

请告诉新 Codex 完成后需要输出：

- 修改了哪些文件
- 实现了哪些功能
- 如何运行
- 如何测试
- 是否还有遗留问题
- 下一步建议

---

# 输出要求

1. 只输出“新 Codex 会话接手提示词”本身。
2. 不要输出解释说明。
3. 不要说“下面是提示词”。
4. 不要继续写代码。
5. 不要省略关键上下文。
6. 不要编造不存在的信息。
7. 如果上下文中没有的信息，请明确标注“当前上下文未明确”。
8. 新提示词必须可以直接复制到新 Codex 会话中使用。
9. 新提示词要尽量完整、具体、可执行。
10. 请优先使用本会话的真实上下文，而不是通用模板。

现在请生成这份“新 Codex 会话接手提示词”。
